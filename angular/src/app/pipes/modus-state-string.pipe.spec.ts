import { ModusStateStringPipe } from './modus-state-string.pipe';

describe('ModusStateStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ModusStateStringPipe();
    expect(pipe).toBeTruthy();
  });
});
